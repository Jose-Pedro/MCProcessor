<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <MessagePopover id="messagePopoverSearch"
        items="{message>/}"
        afterClose="onMessagePopoverSearchClose"
        activeTitlePress="onMessagePress"
        initiallyExpanded="true"
        groupItems="true">
        <MessageItem id="messageItemSearch"
            type="{message>type}"
            title="{message>message}"
            activeTitle="{parts: [{path: 'message>controlIds'}], formatter: '.isPositionable'}"
            subtitle="{message>additionalText}"
            description="{message>description}"/>
    </MessagePopover>
</core:FragmentDefinition>